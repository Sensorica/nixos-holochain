result
result-*
.direnv/
*.happ

.local
*.local.*

/Plans

# Private material never enters git; *.example files document the layout
/secrets/*
!/secrets/*.example

# Git worktrees used by the PM/Builder workflow
.worktrees/
